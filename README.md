# Windows-Server-add-DNS-IIS-Lab
This section explains the **step-by-step configuration performed entirely on a single Host Server machine**.   The Host Server is responsible for **IP configuration, IIS setup, DNS configuration, and website hosting**.
